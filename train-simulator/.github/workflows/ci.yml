name: Build and test
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json
      - run: dotnet restore train-simulator.slnx
      - run: dotnet build train-simulator.slnx -c Release --no-restore
      - run: dotnet test train-simulator.slnx -c Release --no-build --logger trx
